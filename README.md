# Decktron
Electron-based X browser with panes and multi-account support.
Work in progress.


### Run

```shell
npm install
npm start
```

### Build
```shell
npm package
```

Artifacts will appear in `out` folder.